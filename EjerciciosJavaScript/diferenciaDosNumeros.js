    let num_uno=4;
    let num_dos=8;
    if (num_uno > num_dos) {
        let dif=num_uno-num_dos;
        console.log("La diferencia es "+dif);
    } else if (num_dos > num_uno) {
        let dif=num_dos-num_uno;
        console.log("La diferencia es "+dif);
    }else{
        console.log("No hay diferencia");
    }